# AVD Profiles

Profiles describe Android Emulator configurations but do not contain Google system images. Use `tools/android/bootstrap-arm64.sh` to provision the official SDK components and `tools/android/create-avd.sh` to materialize an AVD locally.
